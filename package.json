{
  "name": "spotify-clone",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "package:desktop": "lerna run --scope desktop --stream dev",
    "package:mobile": "lerna run --scope mobile --stream dev",
    "start": "run-p --print-label package:*",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-conventional": "^13.1.0",
    "husky": "^7.0.2",
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "next": "^11.1.2",
    "next-transpile-modules": "^8.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
